<form method="post" action="{{ path('cms_concorso_article_delete', {'id': concorso_article.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ concorso_article.id) }}">
    <button class="btn">Delete</button>
</form>
